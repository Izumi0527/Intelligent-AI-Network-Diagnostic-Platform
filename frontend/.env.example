# 前端环境变量样例 — 复制为 frontend/.env.local 后填入真实值
# .env.local 已被根 .gitignore 排除，不会被提交

# ─── 内部 API 鉴权 token（必填） ────────────────────────────
# 与后端 .env 中的 INTERNAL_API_TOKEN 保持一致，前端调用 /api 时通过
# X-Internal-Api-Token 请求头自证身份。
# 当前消费点：src/utils/terminalService.ts、src/utils/aiService.ts
VITE_INTERNAL_API_TOKEN=change-me-internal-api-token

# ─── 可选：未来扩展占位（当前代码未消费，新增功能时按需启用） ─────
# 仅作为约定示例，添加任何 VITE_* 时必须在 src/ 内有真实消费点。
# VITE_API_BASE_URL=/api
# VITE_DEFAULT_THEME=light
# VITE_DEFAULT_AI_MODEL=claude-sonnet-4-6
